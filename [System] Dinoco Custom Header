/* ซ่อน Admin Bar สำหรับสมาชิกทั่วไป (แอดมินยังเห็นอยู่) */
add_action('after_setup_theme', 'dinoco_remove_admin_bar');

function dinoco_remove_admin_bar() {
    if (!current_user_can('administrator') && !is_admin()) {
      show_admin_bar(false);
    }
}
