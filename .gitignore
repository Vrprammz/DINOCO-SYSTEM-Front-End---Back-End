node_modules/
dist/
.DS_Store
*.pdf
*.xlsx

# Second Brain wiki (symlink to external knowledge base, do not commit)
.second-brain

# Claude Code local plugin state
.claude/plugins/

# Export snapshots — baseline data commits go in subdirs; raw curls ignored
.export/*.tmp
.export/scratch/

# IDE / tool caches
.obsidian/
.phpunit.cache/
.phpunit.result.cache
junit-*.xml
coverage/
clover.xml

# Composer / PHP deps (composer.json + composer.lock are tracked)
vendor/

# Per-run export artifacts (CSVs + catalog dumps from audit scripts)
b2f-migration-dry-run-*.csv
dinoco-catalog-*/

# GitHub Actions workflow scratch dir (real workflows live in .github/)
github/
